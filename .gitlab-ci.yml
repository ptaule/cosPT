image: ubuntu:latest

stages:
  - build
  - test

build:
  stage: build
  before_script:
    - apt-get update -qq && apt-get install -y -qq gsl-bin libcuba3 libconfig++-dev
  script:
    - make clean && make -j
  artifacts:
    paths:
      - build

test:
  stage: test
  script:
    ./build/cosPT
